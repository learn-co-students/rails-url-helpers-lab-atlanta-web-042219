<h1><%= @student.to_s %></h1>
<h3><%= @student.active ? 'This student is currently active.' : 'This student is currently inactive.' %></h3>

<% if @student.active
	activate_msg = 'Deactivate'
else
	activate_msg = 'Activate'
end %>
<%= link_to activate_msg, activate_student_path, method: :get %><br>
<%= link_to 'Home', students_path %>